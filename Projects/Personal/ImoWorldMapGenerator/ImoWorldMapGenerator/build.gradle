ext {
    mainClass = 'com.personal.imo.AppStart' + project.name
    projectVersion = '1.0.0'

    subProjectNames = [':xml-utils']
    compileClassPaths = []
    compileJarFilePaths = []
    testCompileClassPaths = []
    testCompileJarFilePaths = []
}

apply from: gradle.ext.rootDirPath + '/common_build.gradle'
